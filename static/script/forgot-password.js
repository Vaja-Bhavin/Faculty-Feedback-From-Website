document.addEventListener("DOMContentLoaded",()=>{
    // const otpbtn = document.querySelector(".send-otp");
    const otpdiv = document.querySelector(".otpdiv");
    const lastbtn = document.querySelector(".lastbtn-box");
    const id = document.querySelector(".id")

    const next = () => {
        // otpdiv.style.display = "flex"
        // lastbtn.style.display = "flex"
        // otpbtn.disabled = true
        // id.value = "verify_otp"
}
    // otpbtn.addEventListener("click",next);
});

